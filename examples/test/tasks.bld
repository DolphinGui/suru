SOMEVARIABLE = $(LINKFLAGS)

a: main.o lib.o

main.o: main.cpp config.h

lib.o: lib.c config.h

config.h: makeheader.sh config.h.in

% < %.o
	g++ -o $@ $^ -O3 $(SOMEVARIABLE)

%.h < %.sh %.h.in
	$^ $@
